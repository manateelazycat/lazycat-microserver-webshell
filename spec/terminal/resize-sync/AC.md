# 场景 1：多个窗口和设备交替使用终端时尺寸与输入恢复正常
ID: SC-MULTI-DEVICE-RESIZE-SYNC
Profile: standard
Gate: required
Given 三个或更多授权浏览器在不同窗口或设备上打开同一标签中的终端
When 用户依次切换操作窗口、输入或调整窗口大小，并在一次连接恢复期间输入命令
Then 终端尺寸适应最后发生用户操作的窗口，连接恢复后输入按顺序执行且不重复
And 其他窗口同步显示终端内容，不因没有尺寸控制权而持续加载或反复重连
And 被动接收输出、尺寸同步或其他窗口退出不会自行抢占尺寸控制权
